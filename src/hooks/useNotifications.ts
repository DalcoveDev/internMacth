import { useNotifications as useNotificationsInternal } from '../contexts/NotificationContext';

// Re-export the hook with the same name for backward compatibility
export { useNotificationsInternal as useNotifications };